import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const Color primary = Color(0xFF4A148C); // Deep Purple
  static const Color primaryVariant = Color(0xFF7C43BD);
  static const Color secondary = Color(0xFF00BCD4); // Cyan Accent
  static const Color secondaryVariant = Color(0xFF008BA3);

  // Neutral shades
  static const Color lightBackground = Colors.white;
  static const Color darkBackground = Color(0xFF121212);

  static const Color lightSurface = Colors.white;
  static const Color darkSurface = Color(0xFF1E1E1E);

  // Text colors
  static const Color textPrimaryLight = Colors.black87;
  static const Color textSecondaryLight = Colors.black54;
  static const Color textPrimaryDark = Colors.white70;
  static const Color textSecondaryDark = Colors.white60;

  // Error
  static const Color errorLight = Colors.red;
  static const Color errorDark = Colors.redAccent;
}
